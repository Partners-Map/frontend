import styled from 'styled-components';

export const SberLineStyle = styled.div`
  position: absolute;
  width: 100%;
  height: 6px;
  flex-shrink: 0;
  background: linear-gradient(90deg, #00d900 0%, #fdff00 100%);
  z-index: 1;
`;
